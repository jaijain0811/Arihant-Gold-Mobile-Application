// Type declarations for react-native-dotenv (@env module)
// Variables are injected from .env at build time

declare module '@env' {
  export const API_URL: string;
  export const GOOGLE_WEB_CLIENT_ID: string;
  export const NODE_ENV: string;
}
